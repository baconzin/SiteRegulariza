name: Build & Deploy to HostGator

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'   # estável e moderno

      - name: Prep npm (estabilidade de rede)
        run: |
          npm config set audit false
          npm config set fund false
          npm config set fetch-retries 5
          npm config set fetch-retry-factor 2
          npm config set fetch-retry-maxtimeout 60000

      - name: Install dependencies
        working-directory: frontend
        run: npm install --legacy-peer-deps --no-audit --no-fund

      # >>> PASSO-CHAVE: instala peer deps exigidas pelo webpack/schema-utils
      - name: Ensure webpack peer deps (ajv & ajv-keywords)
        working-directory: frontend
        run: npm install --no-save ajv@^8 ajv-keywords@^5 --legacy-peer-deps

      - name: Build React app
        working-directory: frontend
        run: npm run build

      - name: Deploy via FTP to HostGator
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: frontend/build/
          server-dir: /public_html/
          exclude: |
            **/.git*
            **/.github/**
            **/.htaccess
          dangerous-clean-slate: false
